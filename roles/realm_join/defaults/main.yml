---
foreman_realm: "{{ ansible_facts['domain'] | upper }}"
foreman_realm_domain: "{{ ansible_facts['domain'] }}"
foreman_realm_directory_admin_name: "Administrator"
foreman_realm_directory_admin_password: "changeme"
