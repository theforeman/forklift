- name: 'Install bootdisk plugin'
  include_role:
    name: plugins/plugin_installer
  vars:
    plugin_installer_options: >
      --enable-foreman-plugin-bootdisk
      {{ foreman_bootdisk_installer_options }}
