- name: Ensure nss-myhostname is installed
  package:
    name: libnss-myhostname
    state: present
  # On EL7 it's always installed
  when: ansible_facts['os_family'] == "Debian" and ansible_facts['distribution_release'] != 'xenial'
