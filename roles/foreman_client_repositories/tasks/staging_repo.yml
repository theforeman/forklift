---
- name: 'Remove foreman-client-release'
  yum:
    name: foreman-client-release
    state: absent
  tags:
    - packages

- name: 'Foreman {{ foreman_client_repositories_version }} Client Staging Repository'
  yum_repository:
    name: foreman-client-staging
    description: "Foreman {{ foreman_client_repositories_version }} Client Staging Repository"
    baseurl: "https://stagingyum.theforeman.org/client/{{ foreman_client_repositories_version }}/{{ foreman_client_repositories_dists[ansible_facts['os_family']] }}{{ ansible_facts['distribution_major_version'] }}/x86_64/"
    gpgcheck: no
  tags:
    - packages
