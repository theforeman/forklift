- name: 'Install templates plugin'
  shell: >
    foreman-installer
    --enable-foreman-plugin-templates
    {{ templates_installer_options }}
  tags:
    - installation

- name: 'Fetch community templates'
  shell: >
    foreman-rake templates:sync {{ templates_sync_params }}
  when: templates_sync
  tags:
    - seed

